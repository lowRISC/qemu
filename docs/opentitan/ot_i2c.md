# OpenTitan I2C

* `-device <name>,bus=<bus>,address=<address>` can be used to attach devices at a specific address
  to one of the three I2C buses.

  Bus names depends on the platform. They are usually named as `ot-i2c0` ... `ot-i2cN`.

See also [I2C host proxy](i2c_host_proxy.md)
